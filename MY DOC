USEFUL PHP ARTISAN COMMANDS
-----------------------------
# Start the local development server
php artisan serve

# Clear application cache
php artisan cache:clear

# Run database migrations
php artisan migrate

# Rollback the last database migration
php artisan migrate:rollback

# Create a new controller
php artisan make:controller ControllerName

# Create a new model
php artisan make:model ModelName

# Create a new migration
php artisan make:migration create_table_name_table

# List all available Artisan commands
php artisan list

# Show help for a specific command
php artisan help command-name

# Run database seeders
php artisan db:seed

# Clear and cache routes
php artisan route:clear
php artisan route:cache

# Clear and cache config
php artisan config:clear
php artisan config:cache

# Clear compiled views
php artisan view:clear


-------------------------------
USEFUL  GIT COMMANDS
-------------------------------
# Initialize a new git repository
git init

# Check the status of your files
git status

# Add all changes to staging
git add .

# Add a specific file to staging
git add filename

# Commit staged changes with a message
git commit -m "Your commit message"

# View commit history
git log

# Create a new branch
git branch branch-name

# Switch to a branch
git checkout branch-name

# Create and switch to a new branch
git checkout -b branch-name

# Merge a branch into the current branch
git merge branch-name

# Push changes to remote repository
git push origin branch-name

# Pull latest changes from remote repository
git pull origin branch-name

# Clone a remote repository
git clone repository-url

# Remove a file from git and disk
git rm filename

# Remove a file from git but keep it locally
git rm --cached filename

# Show changes between commits, branches, etc.
git diff

# Stash changes for later
git stash

# Apply stashed changes
git stash apply
# ...existing



============================================
-------------XAMPP--------------------------
============================================
🟢 1. Set MySQL Root Password

By default, MySQL in XAMPP has no password for the root user.

Steps:

Start Apache and MySQL in the XAMPP Control Panel.

Open your browser and go to:

http://localhost/phpmyadmin/


Click on the “User accounts” tab.

Find the root user (usually listed as root@localhost).

Click “Edit privileges” → scroll down to “Change password” section.

Enter your new password and click “Go”.

Alternatively (via command line):

Open the XAMPP Shell (click the “Shell” button in XAMPP Control Panel).

Run the following commands:

mysql -u root


Then:

ALTER USER 'root'@'localhost' IDENTIFIED BY 'YourNewPassword';
FLUSH PRIVILEGES;
EXIT;

🟡 2. Update phpMyAdmin Configuration

After setting the MySQL password, phpMyAdmin will need it to log in.

Steps:

Open this file in a text editor:

C:\xampp\phpMyAdmin\config.inc.php


Find this line:

$cfg['Servers'][$i]['auth_type'] = 'config';
$cfg['Servers'][$i]['user'] = 'root';
$cfg['Servers'][$i]['password'] = '';


Replace it with:

$cfg['Servers'][$i]['password'] = 'YourNewPassword';


(Optional but safer) Change auth type to cookie:

$cfg['Servers'][$i]['auth_type'] = 'cookie';


→ This will prompt you to log in manually each time you access phpMyAdmin.
==============================================